# frozen_string_literal: true

RSpec.describe Modus do
  it "has a version number" do
    expect(Modus::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
